import { Game, Player } from "../../../../../packages/shared";

export const createNewGame = (withOpenAI: boolean, auth: ) => {

    const players: Player[] = []


    const game: Game = {
      
  };
};
